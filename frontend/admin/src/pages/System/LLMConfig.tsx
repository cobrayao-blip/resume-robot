import React from 'react';
import { Card } from 'antd';

const LLMConfig: React.FC = () => {
  return (
    <Card>
      <h1>LLM配置</h1>
      <p>LLM配置页面（开发中）</p>
    </Card>
  );
};

export default LLMConfig;

