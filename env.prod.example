# ============================================
# ResumeAI Platform - 生产环境配置示例
# ============================================
# 复制此文件为 .env 并修改所有配置值
# cp env.prod.example .env
# ============================================

# ============================================
# 数据库配置（必需）
# ============================================
POSTGRES_USER=resume_user
POSTGRES_PASSWORD=your-strong-database-password-here
POSTGRES_DB=resume_db
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# ============================================
# Redis 配置（必需）
# ============================================
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=your-redis-password-here

# ============================================
# MongoDB 配置（必需）
# ============================================
MONGO_USER=resume_user
MONGO_PASSWORD=your-strong-mongodb-password-here
MONGODB_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongodb:27017/resume_robot?authSource=admin

# ============================================
# Milvus 配置（必需 - 向量数据库）
# ============================================
MILVUS_HOST=milvus
MILVUS_PORT=19530

# ============================================
# MinIO 配置（必需 - Milvus依赖）
# ============================================
MINIO_ACCESS_KEY=your-minio-access-key-here
MINIO_SECRET_KEY=your-minio-secret-key-here

# ============================================
# 安全配置（必需 - 生产环境必须设置）
# ============================================
# 生成强密钥：python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-min-32-chars-change-in-production

# 调试模式（生产环境必须为False）
DEBUG=False

# ============================================
# CORS 配置（必需 - 限制为实际域名）
# ============================================
# 只允许你的实际域名，不要使用通配符
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ============================================
# DeepSeek API 配置
# ============================================
# 注意：生产环境建议在管理后台配置，而不是使用环境变量
# 如果使用环境变量，请确保安全存储
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ============================================
# 应用配置
# ============================================
PROJECT_NAME=ResumeAI Platform
VERSION=1.0.0

# ============================================
# 文件上传和导出配置
# ============================================
EXPORT_DIR=/tmp/export
UPLOAD_MAX_MB=10

# ============================================
# JWT 配置
# ============================================
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================
# 前端配置
# ============================================
VITE_API_BASE=/api/v1

# ============================================
# 生产环境注意事项
# ============================================
# 1. 所有密码和密钥必须使用强密码
# 2. SECRET_KEY 必须至少32字符
# 3. DEBUG 必须设为 False
# 4. CORS_ORIGINS 必须限制为实际域名
# 5. 数据库密码必须足够强
# 6. 建议启用Redis密码认证
# 7. MongoDB密码必须足够强
# 8. MinIO访问密钥必须足够强
# 9. 定期备份数据库（PostgreSQL、MongoDB）
# 10. 配置SSL/TLS证书
# 11. 设置防火墙规则
# 12. 配置日志轮转和监控
# 13. Milvus数据需要定期备份

